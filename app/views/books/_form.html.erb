<%= form_with(model: [@book_list, @book]) do |form| %>
  <% if book.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% book.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="d-flex flex-column gap-2">
  <div>
    <%= form.label :bookTitle, style: "display: block" %>
    <%= form.text_field :bookTitle, required: true, class: "form-control"%>
  </div>

  <div>
    <%= form.label :author, style: "display: block" %>
    <%= form.text_field :author, required: true, class: "form-control"%>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description, required: true, class: "form-control" %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.text_field :price, required: true, class: "form-control" %>
  </div>

  <div>
    <%= form.submit class: "btn btn-danger" %>
  </div>
</div>
<% end %>
